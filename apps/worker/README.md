# Worker App

`@ainyc/aeo-platform-worker` is the background job skeleton for the platform. Phase 1 only provides a typed startup path and heartbeat logging so scheduling and provider execution can be layered in later without inventing the package structure again.
